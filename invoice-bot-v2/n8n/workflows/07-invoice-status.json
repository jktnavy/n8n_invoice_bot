{
  "name": "07-invoice-status",
  "active": false,
  "nodes": [
    {
      "parameters": {},
      "id": "execute-workflow-trigger",
      "name": "Execute Workflow Trigger",
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1,
      "position": [
        0,
        0
      ]
    },
    {
      "parameters": {
        "jsCode": "const invoiceNumber = String($json.invoice_number ?? '').trim() || null;\nconst explicitInvoiceId = $json.invoice_id || null;\nconst fallbackInvoiceId = $json.last_invoice_id || null;\nconst invoiceId = invoiceNumber ? null : (explicitInvoiceId || fallbackInvoiceId || null);\n\nif (!invoiceId && !invoiceNumber) {\n  throw new Error('invoice identifier is required');\n}\n\nreturn [\n  {\n    json: {\n      ...$json,\n      invoice_id: invoiceId,\n      invoice_number: invoiceNumber,\n      status_lookup_requested: true,\n    },\n  },\n];\n"
      },
      "id": "status-guard",
      "name": "Status Guard",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        240,
        0
      ]
    }
  ],
  "connections": {
    "Execute Workflow Trigger": {
      "main": [
        [
          {
            "node": "Status Guard",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "staticData": {
    "description": "Query database for invoice and latest delivery state. LLM must never guess status."
  }
}
